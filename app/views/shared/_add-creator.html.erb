<div class="container show-form"">
  <% if @youtube_data.nil? %>
    <div class="text-center">
      <h1>
        Sorry, we couldn't find your creator. Please verify the URL and try again!
      </h1>
      <hr>
      <div class="btn btn-primary btn-lg btn-submit-home">
        <%= link_to "Add a new creator", "#creator-modal", "data-toggle" => "modal", class: '' %>
        <%= render 'shared/modal_creator' %>
      </div>
    </div>
  <% else %>
    <div class="row">
      <div class="col-xs-6 text-center">
        <h1> Waouw, we found your creator! 🎉🎉🎉</h1>

        <div class="card card-showcased" id="show-form-card">
          <div id="avatar-name-div">

          <%= cl_image_tag @youtube_data[:remote_avatar_photo_url], class: "avatar-showcased-creator" %>

          </div>
          <div class="name-figures"></div>
          <h3 class="showcased-name"> <%=@creator.youtube_name %></h3>
          <p><%= @creator.nb_followers %> Followers </p>
          <p><%= @youtube_data[:nb_videos]%> Videos Uploaded</p>
        </div>
      </div>
      <div class="col-xs-6">
        <h1 class="text-center"> Add some information to your review 🎬🎬🎬</h1>
        <div>
          <% if @creator.errors.messages.any? %>
          <p><%= @creator.errors.full_messages %></p>
          <% end %>
        </div>
        <div class="card form-card">

          <%= simple_form_for @creator, url: create_creators_path do |f| %>
            <!-- [...] -->
            <%= f.error_notification %>
            <div class="form-inputs">
             <%= f.input :description, label: "Write the review about #{@creator.youtube_name}, explain what makes you vibrate..."%>
             <%#= f.input :video_url, label: "Pick a video that embodies the most #{@creator.youtube_name}" %>

             <% @youtube_data[:top_videos].each do |id| %>
                <iframe class="ytplayer-showcased" type="text/html"
                 src="http://www.youtube.com/embed/<%= id %>?autoplay=0"
                 frameborder="0" height="200px" width="300">
                 </iframe>
             <% end %>



              <%#= f.collection_radio_buttons(
    :video_url, @video_array, :first, :last
  ) do |b|
    b.label { b.radio_button + b.label } end %>



             <%= f.input :video_url, :as => :radio_buttons, collection: @video_array, label: "Pick a video that embodies the most #{@creator.youtube_name}" %>



             <%= f.input :tag_list, label: "Add a few tag applying to #{@creator.youtube_name}", hint: "Add several tags separated with commas",  value: f.object.tag_list.to_s %>



             <!-- [Hidden-Fields] -->
             <%= f.hidden_field :channel_url %>
             <%= f.hidden_field :youtube_name %>
             <%= f.hidden_field :remote_avatar_photo_url %>
             <%= f.hidden_field :nb_followers %>
             <%= f.hidden_field :country %>
             <%= f.hidden_field :language, class: " bfh-languages"%>
           </div>
           <!-- [...] -->
           <div class="form-actions text-center">
            <%= f.submit "Add #{@creator.youtube_name}", class: "btn btn-primary" %>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
</div>
